		#print line[i]
		continue
		count+=1
		sys.stdout.write(line[i]+" ")
	#print len(line)
	for i in range(1,len(line)):
	if count == 0:
	line = line.replace("\n","")
	line = line.split()
	sys.stdout.write("\n")
count=0
f  = open("digitdata.txt","r")
for line in f:
import sys